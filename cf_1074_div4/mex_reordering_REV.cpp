#include <bits/stdc++.h>
int main() {
    int t;
    std::cin >> t;
    while (t--) {
        int n;
        std::cin >> n;
        std::vector<long long> vec(n);
        for (int i = 0; i < n; i++) std::cin >> vec[i];
    }
}
